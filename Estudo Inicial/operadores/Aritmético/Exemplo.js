const [v1, v2, v3, v4, v5] = [7, 19, 23, 34, 49]

const soma = v1 + v2 + v3 + v4 + v5;
const subtracao = v2 - v5;
const multiplicacao = v1 * v4;
const divisao = v5 / v1;
const modulo = v5 % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);